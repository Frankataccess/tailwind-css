import './navbar.css'

const NavBar = () => {

    const title = "hello"

    return (
        <header className="app-header">
            <h1>{title}</h1>
        </header>
    );
}

export default NavBar;